import BasicLayouts from "@/components/layouts/BasicLayouts";
import AboutList from "@/components/views/about/AboutList";
import React from "react";

const About = () => {
  return (
    <BasicLayouts>
      <AboutList />
    </BasicLayouts>
  );
};

export default About;
